// Android SDK and dependency versions
// These variables are used across the project for consistent versioning

ext {
    // Android SDK versions
    minSdkVersion = 22
    compileSdkVersion = 34
    targetSdkVersion = 34
    
    // AndroidX versions
    androidxAppCompatVersion = '1.6.1'
    androidxCoordinatorLayoutVersion = '1.2.0'
    androidxCoreVersion = '1.12.0'
    androidxActivityVersion = '1.8.0'
    androidxFragmentVersion = '1.6.2'
    coreSplashScreenVersion = '1.0.1'
    
    // Testing versions
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.1.5'
    androidxEspressoCoreVersion = '3.5.1'
    
    // Kotlin version (if using Kotlin)
    kotlinVersion = '1.9.10'
    
    // Firebase versions are managed by BoM in app/build.gradle
    // No need to specify individual versions here
}
